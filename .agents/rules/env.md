## Applies to
`**`

## Standards
- MUST keep secrets in environment variables only.
- MUST provide a `.env.example` with empty placeholder values.
- MUST NOT commit a real `.env` (it is gitignored).
- SHOULD document each required variable and where to obtain it.
